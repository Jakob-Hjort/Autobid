<UserControl x:Class="autobid.Views.HomeView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<StackPanel Spacing="8">
		<TextBlock Name="WelcomeText" Text="Welcome" FontSize="20"
				   FontWeight="SemiBold" Foreground="{StaticResource Brush.Text}"/>
		<TextBlock Text="(Her kommer listerne Your auctions / Current auctions)"
				   Foreground="{StaticResource Brush.Muted}"/>
	</StackPanel>
</UserControl>
