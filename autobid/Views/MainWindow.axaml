<Window
  x:Class="autobid.Views.MainWindow"
  xmlns="https://github.com/avaloniaui"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  Width="1100" Height="760"
  Background="{StaticResource Brush.Bg}">

	<DockPanel>
		<Border DockPanel.Dock="Top" Background="{StaticResource Brush.Card}" Padding="12"
				BorderBrush="{StaticResource Brush.Border}" BorderThickness="0 0 0 1">
			<TextBlock Name="HeaderText" Text="autobid — Login"
					   Foreground="{StaticResource Brush.Text}" FontSize="18" FontWeight="SemiBold"/>
		</Border>

		<Border Margin="16" Background="{StaticResource Brush.Card}" CornerRadius="12" Padding="16">
			<!-- Her viser vi Login / CreateUser / Home -->
			<ContentControl Name="MainContent"/>
		</Border>
	</DockPanel>
</Window>
